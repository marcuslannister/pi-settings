# pi-settings

Tracked config for the `pi` agent (`~/.pi`). Only `agent/settings.json` and
`agent/models-store.json` are versioned; `auth.json` and session logs are ignored.

## Agent skills

### Issue tracker

GitHub Issues on `marcuslannister/pi-settings`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical five, label string = role name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
